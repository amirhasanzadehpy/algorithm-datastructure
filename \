package problems

import "github.com/ashanbrown/makezero/makezero"

func IsSame(nums1, nums2 []int) bool {
	if len(nums1) != len(nums2) {
		return false
	}

    map := make(map[int]int)
	return false
}
